machine:
  node:
    version: 5.11.1

dependencies:
  pre:
    - npm install -g markdown-pdf

test:
  override:
    - exit 0

deployment:
  publish:
    branch: master
    commands:
      - echo "this is master"
  develop:
    branch: develop
    commands:
      - echo "this is develop?"
      - markdown-pdf --version
